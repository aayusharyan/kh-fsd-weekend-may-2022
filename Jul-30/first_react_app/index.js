const element = document.getElementById("abcd"); //Get the reference of HTML element.
const react_root = ReactDOM.createRoot(element); //Create a react root.
react_root.render(<App />); //Render the App component.